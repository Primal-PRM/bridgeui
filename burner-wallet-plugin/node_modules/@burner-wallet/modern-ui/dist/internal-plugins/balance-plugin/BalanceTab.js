import React from 'react';
import styled from 'styled-components';
import { DataProviders } from '@burner-wallet/ui-core';
import BalanceItem from './BalanceItem';
const { AccountBalance } = DataProviders;
const Row = styled.section `
  padding: 32px 16px;
  margin: 0 -16px;
  overflow-x: scroll;
  white-space: nowrap;
`;
const BalanceRow = ({ defaultAccount, assets }) => (React.createElement(Row, null, assets.map((asset) => (React.createElement(AccountBalance, { key: asset.id, asset: asset.id, account: defaultAccount, render: (data) => (React.createElement(BalanceItem, { asset: asset, balance: data && data.balance, growthRate: (data && data.growthRate) || '0' })) })))));
export default BalanceRow;
//# sourceMappingURL=BalanceTab.js.map